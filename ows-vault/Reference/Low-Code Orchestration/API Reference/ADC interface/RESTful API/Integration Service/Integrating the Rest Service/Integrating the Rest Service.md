---
title: "Integrating the Rest Service"
source: "https://1057-sg-studio.teleows.com/adc-studio-project-mgt/web/rest/help/doc/en_US/zh-cn_topic_300000039.html"
depth: 5
children: ["Inbound REST API for Obtaining the Token for OAuth2 Authentication", "The inbound REST interface is compatible with legacy assets.", "The inbound REST interface is compatible with legacy assets.", "The inbound REST interface is compatible with legacy assets.", "The inbound REST interface is compatible with legacy assets.", "The inbound REST interface is compatible with legacy assets.", "Interface released by the inbound REST interface for external systems", "Interface released by the inbound REST interface for external systems", "Interface released by the inbound REST interface for external systems", "Interface released by the inbound REST interface for external systems", "Interface released by the inbound REST interface for external systems"]
---
# Integrating the Rest Service

-   **[[Inbound REST API for Obtaining the Token for OAuth2 Authentication|Inbound REST API for Obtaining the Token for OAuth2 Authentication]]**  
    
-   **[[The inbound REST interface is compatible with legacy assets.|The inbound REST interface is compatible with legacy assets.]]**  
    
-   **[[The inbound REST interface is compatible with legacy assets.|The inbound REST interface is compatible with legacy assets.]]**  
    
-   **[[The inbound REST interface is compatible with legacy assets.|The inbound REST interface is compatible with legacy assets.]]**  
    
-   **[[The inbound REST interface is compatible with legacy assets.|The inbound REST interface is compatible with legacy assets.]]**  
    
-   **[[The inbound REST interface is compatible with legacy assets.|The inbound REST interface is compatible with legacy assets.]]**  
    
-   **[[Interface released by the inbound REST interface for external systems|Interface released by the inbound REST interface for external systems]]**  
    
-   **[[Interface released by the inbound REST interface for external systems|Interface released by the inbound REST interface for external systems]]**  
    
-   **[[Interface released by the inbound REST interface for external systems|Interface released by the inbound REST interface for external systems]]**  
    
-   **[[Interface released by the inbound REST interface for external systems|Interface released by the inbound REST interface for external systems]]**  
    
-   **[[Interface released by the inbound REST interface for external systems|Interface released by the inbound REST interface for external systems]]**  
    

**Parent topic:** [[Integration Service|Integration Service]]

## Sub-topics

- [[Inbound REST API for Obtaining the Token for OAuth2 Authentication]]
- [[The inbound REST interface is compatible with legacy assets.]]
- [[The inbound REST interface is compatible with legacy assets.]]
- [[The inbound REST interface is compatible with legacy assets.]]
- [[The inbound REST interface is compatible with legacy assets.]]
- [[The inbound REST interface is compatible with legacy assets.]]
- [[Interface released by the inbound REST interface for external systems]]
- [[Interface released by the inbound REST interface for external systems]]
- [[Interface released by the inbound REST interface for external systems]]
- [[Interface released by the inbound REST interface for external systems]]
- [[Interface released by the inbound REST interface for external systems]]
